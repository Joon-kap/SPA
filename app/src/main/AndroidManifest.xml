<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmu.ajou.spa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WebSocketActivity" />
        <activity android:name=".HttpConnection" />
        <activity
            android:name=".HttpConnect2"
            android:label="@string/title_activity_http_connect2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HttpConnection2"></activity>
    </application>

</manifest>